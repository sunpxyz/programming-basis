/*
 * solu_02_04.cpp
 * 2018/2/5
 */

/*
#include <stdio.h>

int main() {
	int n, m, cnt = 0;
	double sum = 0;
	while (1) {
		scanf("%d%d", &n, &m);
		if (n == 0 || m == 0) break;
		for (int i = n; i <= m; ++i) {
			sum += 1.0 / (i * i);
		}
		printf("Case %d: %.5f\n", ++cnt, sum);
		sum = 0;
	}

	return 0;
}
*/

/*
2 4
65536 655360
0 0
 */