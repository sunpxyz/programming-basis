/*
 * solu_02_01.cpp
 * 2018/2/4
 */

/*
#include <stdio.h>

int main() {
	int a, b, c;
	for (int n = 100; n <= 999; ++n) {
		a = n / 100;
		b = n / 10 % 10;
		c = n % 10;
		if (n == a * a * a + b * b * b + c * c * c) {
			printf("%d = %d^3 + %d^3 + %d^3\n", n, a, b, c);
		}
	}

	return 0;
}
*/