/*
 * solu_02_05.cpp
 * 2018/2/4
 */

/*
#include <stdio.h>

int main() {
	int a, b, c, cnt = 0;
	while (1) {
		scanf("%d%d", &a, &b, &c);
		if (a == 0 || b == 0 && c == 0) break;
		
		printf("Case %d: %.f\n", ++cnt, a * b / 1.0);
	}

	return 0;
}
*/